# mitos — 引き継ぎ

**この文書は引き継ぎ用。**セッションをまたいで失われる文脈を置く場所で、
Claude 固有の開発指示（それは CLAUDE.md の役目）ではない。
役目を終えたら消してよい。


過去の作業（決定・PR のレビュー・issue・作業中の会話）を貯めて引く。
Claude Code / Codex からは MCP とフックで、人からはダッシュボードのチャットで引く。

## 目指しているもの

**ダッシュボードのチャットで、ドメイン知識と開発知識の両方に答える。**
「ドメイン知識にめちゃめちゃ強くなってほしい」が最重要要件。

その先は、聞かれてから答えるのではなく**作業を見て先に言う**こと（依頼者の言葉で「JARVIS」）。
編集フックが第一歩で、次は「賢くなっているか」を指標で見ながら選別の質を上げる。

**開発の記録だけを前提に最適化しない。**チャットに来る問いは業務の用語・なぜその仕様か・
誰がいつ決めたか、が混ざる。`node` を多相 1 表にしてあるのはそのため（種別を足しても
ベクトル索引が割れない）。

## 構成

```
server/      取り込み・検索・チャット・MCP・フック（依存は最小、テストは node:test）
dashboard/   React + Vite + TanStack Router + shadcn
plugin/      Claude Code / Codex へ配るもの（skills, hooks, bin, dist）
supabase/    migrations
```

資格情報は `~/.claude/knowledge.env`。**鍵は 3 つに分かれている。**

| ロール | 誰が使うか | 書けるもの |
|---|---|---|
| `postgres` | CLI | 全部 |
| `knowledge_ro` | MCP・フック・API の読み取り | 何も書けない |
| `mitos_cfg` | ダッシュボードの設定 | scope / group / person / term / chat のみ |

**推論する層に書き込みを持たせない。**MCP は PR コメントや issue 本文という外部の文章を読むので、
そこに書き込みを足すと「記録に仕込まれた文言が設定を書き換える」経路ができる。
チャットの用語登録もコールバック経由で、資格情報は `chat.ts` に渡していない。

## 実測して決めたこと

数字は 2026-09-06 時点。**再測定してから引用すること。**

### 検索はハイブリッド（30 問の評価）

| 方式 | recall@5 | MRR |
|---|---:|---:|
| ベクトルのみ | 67% | 0.547 |
| ベクトル + 再ランク | 80% | 0.761 |
| **融合（+pgroonga）+ 再ランク** | **93%** | **0.872** |

ID を含む質問（`OT-4578`、`#2323`）が 2/6 → 5/6。**埋め込みは番号を見分けられない。**

### 自動通知は入れない

Terraform の plan 結果・デプロイ URL・カバレッジ表は推論を含まない。
一時は 34,591 件中 14,852 件（43%）がこれだった。`actor.ts` が名前で判定する。
**AI レビュー（gemini-code-assist / coderabbitai）は落とさない** — 指摘に中身がある。

**PR 本体は bot 作成でも入れる。**リリース PR は `macbeeplanet-dev[bot]` が作るので、
コメント用のフィルタを流用すると 2,011 件（うちマージ済み 1,641 件）が丸ごと消える。

### コードは埋め込まない

取り込んだ瞬間から古くなる。記録は「なぜそうしたか」、コードは「いまどうなっているか」と
役割を分け、`grep_code` / `read_code` で実物を見に行く。**食い違ったらコードが正しい。**

### 発言は「書かれた時点の話」

進捗を聞かれたら `find_issues` / `find_prs` で現在の状態を確かめる。
8/24 のコメントを現在形で読んで「レビュー待ち」と答えたが、PR は 8/25 にマージ済みで
issue は 8/31 に Done だった。**状態を答えるときは必ず現在の状態を根拠にする。**

### ダッシュボードの技術選定

React 19 + Vite（Rolldown）+ TanStack Router / Query + Tailwind 4 + shadcn。
状態管理ライブラリと Storybook は入れない（Router の型付き search params で足りる）。

- **`@tanstack/router-plugin` は `@vitejs/plugin-react` より前**に置く
- React Compiler は `compiler: true`（ネイティブ）。**babel ではなく `oxc-transform-react` が要る**
- TypeScript 7 は `baseUrl` を廃止した。`erasableSyntaxOnly` が要る（Codex は PATH 上の node を使う）
- **バンドルは import の書き方が支配的。**`import * as z from "zod"` は 23KB、
  `import { z }` は 87KB（3.7 倍）。ライブラリ選択より効く

## 踏んだ失敗の型

同じ判断を繰り返さないための記録。どれも「動いたつもりで動いていない」。

**測らずに切り捨てた結論は、データが変われば嘘になる。**
`pgroonga_score` が全行に 1 を返すのを見てハイブリッドを見送ったが、それはノード 127 件のときの
観測だった。26,000 件では 1〜40 に分布する。**「効果を測れないので入れない」と書いたまま
放置したのが誤り。**

**分母を間違えると問いが無意味になる。**
「ファイル紐づき発言 10,404 件のうち何割」ではなく「編集イベントあたりのヒット率」。
直したら 56% で、「沈黙して無価値では」という懸念自体が的外れだった。

**フックは fail-open なので、遅いと「無い」のと同じ。**
編集時の助言が索引なしで 17.5 秒かかり、上限 2.5 秒を超えて黙っていた。
部分索引 2 本（node_pr_in_record / node_utterance_pr）で 519ms になった。
**フックを足したら必ず実際に叩いて測る。**

**ログが空なのを「まだ処理中」と読まない。**
索引作成が 100 分終わらないと思い込んでいたが、索引は既に完成していて、
長引いていたのは後始末だった。**中身を確認せず外形だけで判断した。**

**3 回同じ仮説で失敗したら仮説を変える。**
`ask_term` が呼ばれない原因をプロンプトのせいだと 3 回直したが、
実際はテスト用スクリプトが `learn` を渡していなかっただけだった。

**費用は実測値と突き合わせる。**
キャッシュ済み入力（通常の 10%）を数えず 42% 過大に報告した。
**測る前に最適化を提案したのも誤り** — 実際は 95% がキャッシュ済みで既に 9 割引だった。

**一括の書き込みを 1 日に詰め込むと IO 予算が尽きる。**
40,000 行の挿入・27,000 行の更新・17,000 行の削除・HNSW 再構築を同日にやり、
Supabase から Disk IO の警告が来た。**HNSW は行を消しても縮まない**（REINDEX が要る）。

**背景ジョブを起動しすぎる。**
完了済みの処理を待ち続ける残骸を含め、待機ループを 3 つ同時に走らせていた。

## 運用

```
mitos sync              登録済みの取り込み元をまとめて更新（launchd が毎日 6:00 に実行）
mitos doctor            資格情報と接続、Linear MCP の疎通
mitos advice            編集時の助言が効いているか（ヒット率・再提示率）
mitos usage             OpenAI の使用量
mitos who               誰が誰かの名簿
```

日次同期は `~/Library/LaunchAgents/com.mitos.sync.plist`。ログは `~/.claude/mitos-sync.log`。
外すときは `launchctl bootout gui/$(id -u)/com.mitos.sync`。

**Linear は API キーが発行できない**（組織で禁止）ので、OAuth 済みの MCP を `claude -p` の
ヘッドレス実行で叩く。`--output-format stream-json` からツール結果を生で拾うので、
issue 本文もコメントも LLM を通らない。ページ送りのカーソルも呼び出し側が読む。

## いまの回答精度（2026-09-06 時点・忖度なし）

**総合 70 点。**内訳と、そう判断した根拠。

| 面 | 点 | 根拠 |
|---|---:|---|
| 引ける（検索） | 90 | 30 問で recall@5 93%。**ただし評価セットは自作**なので割り引くべき |
| 答えが正しい | 70 | 実地で 25 問ほど試して**欠陥を 6 件**見つけた。うち半分は人に指摘されて気付いた |
| 網羅している | 50 | 下の「入っていないもの」参照 |

### いちばん重い問題: 答えの正しさを測っていない

**93% は検索の数字であって、答えの数字ではない。**`evals/chat.ts` が見ているのは
引用の健全性（存在しない番号を引いていないか）だけで、**事実として正しいかは測っていない。**
つまり「回答精度 N%」と言える根拠が無い。上の 70 点は推定であって実測ではない。

**評価セットが自作なのも弱い。**質問も期待値も同じ人間（AI）が書いたので、
コーパスに何があるか知ったうえで質問を作っている。実際の質問はもっと外れる。
**チャット履歴（`chat` 表）に実際の質問が貯まり始めているので、そこから作り直すべき。**

### 分かっている弱点

- **人名の質問がいちばん弱い**（評価で 2/3）。名簿の 11 人のうち 3 人（永井さん・矢野さん・
  増田さん）は**ハンドル名の類似だけで対応付けた推測**。メールで裏を取っていない
- **AI レビューが全体の 28%**（9,358 件）。gemini-code-assist と coderabbitai の指摘は
  中身があるので残したが、**それが答えの質を上げているか下げているかを測っていない**
- **編集フックの選別が粗い。**距離順に並べているだけで、「普遍的な指摘」と
  「その場限りの質問」を区別できていない。実測では 3 件中 1 件しか本当に役立たなかった
- **陳腐化は issue と PR にしか効いていない。**「そのコードは後で書き換えられた」は検知できない
- **ハルシネーションの率を測っていない。**存在しない issue を 1 回試しただけ

### 入っていないもの（網羅が 50 点な理由）

- **他人の Linear issue。**入っているのは自分が担当か起票の 22 件だけ。
  チーム全体は 4,393 件あり、「◯◯さんの issue は」に答えられない
- **PR の作成日。**既存 8,123 件の attrs に `createdAt` が無い（今後の取り込み分だけ入る）。
  「作成順で最新」に厳密には答えられず、マージ日順で代用している
- **MTG 文字起こしの後半。**貼り付けが 12,000 字で切れる。分割していない
- **monopoly-source 以外のセッション記録。**取り込んだのは 1 リポジトリぶんだけ
- **圧縮要約 4 件**がセッション記録に残っている（除去に失敗したまま）

### 次にやるなら、この順

1. **答えの正しさを測る仕組み。**チャット履歴から golden set を作り、
   事実の正誤をゲートにする。**これが無いと以降の改善が良くなったか分からない**
2. 編集フックの選別（指標が貯まってから。距離以外の手がかりを足す）
3. 他人の Linear issue（全 4,393 件。まとめ叩きで 5 時間、夜間なら回せる）
4. 名簿の推測 3 人を裏取り、AI レビューの寄与を測る
