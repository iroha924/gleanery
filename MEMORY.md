# mitos — 引き継ぎ

**この文書は引き継ぎ用。**セッションをまたいで失われる文脈を置く場所で、
Claude 固有の開発指示（それは CLAUDE.md の役目）ではない。
役目を終えたら消してよい。


過去の作業（決定・PR のレビュー・issue・作業中の会話）を貯めて引く。
Claude Code / Codex からは MCP とフックで、人からはダッシュボードのチャットで引く。

## 目指しているもの

**ダッシュボードのチャットで、ドメイン知識と開発知識の両方に答える。**
「ドメイン知識にめちゃめちゃ強くなってほしい」が最重要要件。

その先が本命で、聞かれてから答えるのではなく**作業を見て先に言う**ことにある。
依頼者の言葉で「アイアンマンの JARVIS」。次の節がその中身。

**開発の記録だけを前提に最適化しない。**チャットに来る問いは業務の用語・なぜその仕様か・
誰がいつ決めたか、が混ざる。`node` を多相 1 表にしてあるのはそのため（種別を足しても
ベクトル索引が割れない）。

## 最重要: JARVIS（聞かれる前に言う）

**検索は手段であって目的ではない。**目指しているのは「こうした方が良いです」と先に言うこと。
依頼者の言葉:

> ゆくゆくはただの検索ではなく自動で開発のアドバイスを…「こうした方が良いです」みたいな
> 感じにできると最高。目指すはアイアンマンの JARVIS

**使えば使うほど賢くなる**ことも同じ要件に含まれる。記録が増えるほど言えることが増える形にする。

### いまどこまで来ているか

第一歩は**編集フック**（`PreToolUse` の `Edit|Write|NotebookEdit`）。これから触るファイルに
ついて、マージ済み PR で言われたことを最大 2 件出す。実測値は `mitos advice` で見る。

```
フックが走った編集   3 回
助言を出せた         1 回（33%）
1 回あたりの候補     1.7 件
同じ助言の再提示率   0%（低いほどよい）
```

**まだ母数が小さく、質を語れる段階にない。**分母（走った編集の数）を貯めるのが先。

### Codex と合意した設計（変えるなら理由が要る）

| 決めたこと | なぜ |
|---|---|
| **ファイル完全一致 + マージ済み PR + 行の距離**で選ぶ | 意味検索は編集のたびに走らせるには遅く、外したときの害が大きい |
| **既定は黙る（abstain by default）** | 出し過ぎると読まれなくなる。沈黙のコストより誤爆のコストが高い |
| **編集時に埋め込みを作らない** | 上限 2.5 秒。フックは fail-open なので、遅いことは「無い」のと同じ |
| **指標を先に、機能を後に** | 「賢くなったか」を測れないまま選別を足すと、良くなったかが分からない |
| ブランチ名を起点にする案（B トリガー）は**見送り** | 効果が未検証。A（ファイル）の指標が貯まってから |

### 次に効きそうな順

1. **分母を貯める**（フックが走った編集の数）。いまの 3 回では何も言えない
2. **選別の質**を上げる。いまは行の距離で並べるだけ。実測で 3 件中 1 件しか役立たなかった
3. **「役に立ったか」を取る手段が無い。**出したことは記録しているが、
   採用されたかは分からない。ここが埋まらないと「賢くなっている」を主張できない
4. 陳腐化の検知。いまは issue と PR にしか効かず、「そのコードは後で書き換えられた」は分からない

**やってはいけないこと。**指標が無いまま助言の種類を増やす。
沈黙率が下がったことを「賢くなった」と読む（誤爆が増えただけかもしれない）。

## 構成

```
server/      取り込み・検索・チャット・MCP・フック（依存は最小、テストは node:test）
dashboard/   React + Vite + TanStack Router + shadcn
plugin/      Claude Code / Codex へ配るもの（skills, hooks, bin, dist）
supabase/    migrations
```

資格情報は `~/.claude/knowledge.env`。**鍵は 3 つに分かれている。**

| ロール | 誰が使うか | 書けるもの |
|---|---|---|
| `postgres` | CLI | 全部 |
| `knowledge_ro` | MCP・フック・API の読み取り | 何も書けない |
| `mitos_cfg` | ダッシュボードの設定 | scope / group / person / term / chat のみ |

**推論する層に書き込みを持たせない。**MCP は PR コメントや issue 本文という外部の文章を読むので、
そこに書き込みを足すと「記録に仕込まれた文言が設定を書き換える」経路ができる。
チャットの用語登録もコールバック経由で、資格情報は `chat.ts` に渡していない。

## 実測して決めたこと

数字は 2026-09-06 時点。**再測定してから引用すること。**

### 検索はハイブリッド（30 問の評価）

| 方式 | recall@5 | MRR |
|---|---:|---:|
| ベクトルのみ | 67% | 0.547 |
| ベクトル + 再ランク | 80% | 0.761 |
| **融合（+pgroonga）+ 再ランク** | **93%** | **0.872** |

ID を含む質問（`OT-4578`、`#2323`）が 2/6 → 5/6。**埋め込みは番号を見分けられない。**

### 自動通知は入れない

Terraform の plan 結果・デプロイ URL・カバレッジ表は推論を含まない。
一時は 34,591 件中 14,852 件（43%）がこれだった。`actor.ts` が名前で判定する。
**AI レビュー（gemini-code-assist / coderabbitai）は落とさない** — 指摘に中身がある。

**PR 本体は bot 作成でも入れる。**リリース PR は `macbeeplanet-dev[bot]` が作るので、
コメント用のフィルタを流用すると 2,011 件（うちマージ済み 1,641 件）が丸ごと消える。

### コードは埋め込まない

取り込んだ瞬間から古くなる。記録は「なぜそうしたか」、コードは「いまどうなっているか」と
役割を分け、`grep_code` / `read_code` で実物を見に行く。**食い違ったらコードが正しい。**

### 発言は「書かれた時点の話」

進捗を聞かれたら `find_issues` / `find_prs` で現在の状態を確かめる。
8/24 のコメントを現在形で読んで「レビュー待ち」と答えたが、PR は 8/25 にマージ済みで
issue は 8/31 に Done だった。**状態を答えるときは必ず現在の状態を根拠にする。**

### ダッシュボードの技術選定

React 19 + Vite（Rolldown）+ TanStack Router / Query + Tailwind 4 + shadcn。
状態管理ライブラリと Storybook は入れない（Router の型付き search params で足りる）。

- **`@tanstack/router-plugin` は `@vitejs/plugin-react` より前**に置く
- React Compiler は `compiler: true`（ネイティブ）。**babel ではなく `oxc-transform-react` が要る**
- TypeScript 7 は `baseUrl` を廃止した。`erasableSyntaxOnly` が要る（Codex は PATH 上の node を使う）
- **バンドルは import の書き方が支配的。**`import * as z from "zod"` は 23KB、
  `import { z }` は 87KB（3.7 倍）。ライブラリ選択より効く

## 踏んだ失敗の型

同じ判断を繰り返さないための記録。どれも「動いたつもりで動いていない」。

**測らずに切り捨てた結論は、データが変われば嘘になる。**
`pgroonga_score` が全行に 1 を返すのを見てハイブリッドを見送ったが、それはノード 127 件のときの
観測だった。26,000 件では 1〜40 に分布する。**「効果を測れないので入れない」と書いたまま
放置したのが誤り。**

**分母を間違えると問いが無意味になる。**
「ファイル紐づき発言 10,404 件のうち何割」ではなく「編集イベントあたりのヒット率」。
直したら 56% で、「沈黙して無価値では」という懸念自体が的外れだった。

**フックは fail-open なので、遅いと「無い」のと同じ。**
編集時の助言が索引なしで 17.5 秒かかり、上限 2.5 秒を超えて黙っていた。
部分索引 2 本（node_pr_in_record / node_utterance_pr）で 519ms になった。
**フックを足したら必ず実際に叩いて測る。**

**ログが空なのを「まだ処理中」と読まない。**
索引作成が 100 分終わらないと思い込んでいたが、索引は既に完成していて、
長引いていたのは後始末だった。**中身を確認せず外形だけで判断した。**

**3 回同じ仮説で失敗したら仮説を変える。**
`ask_term` が呼ばれない原因をプロンプトのせいだと 3 回直したが、
実際はテスト用スクリプトが `learn` を渡していなかっただけだった。

**費用は実測値と突き合わせる。**
キャッシュ済み入力（通常の 10%）を数えず 42% 過大に報告した。
**測る前に最適化を提案したのも誤り** — 実際は 95% がキャッシュ済みで既に 9 割引だった。

**一括の書き込みを 1 日に詰め込むと IO 予算が尽きる。**
40,000 行の挿入・27,000 行の更新・17,000 行の削除・HNSW 再構築を同日にやり、
Supabase から Disk IO の警告が来た。**HNSW は行を消しても縮まない**（REINDEX が要る）。

**自己評価を推定で出さない。**
「回答精度は 70 点」と答えたが、実測したら 92 点だった。**開発中に見つけた欠陥の数（6/25）を、
直した後の精度として使っていた。**測る仕組みを作ってから答えれば 20 分で済んだ。
「測れていない」と言うか、測ってから言うかの二択で、推定を数字として出すのが最悪。

**見積もりも同じ。**「遡り更新は 180MB」と言ったが実測 93MB だった
（埋め込みは TOAST に外出しされ 4,100 バイトに圧縮されるので、attrs だけの更新では書き換わらない）。
**行数も 8,123 件と言ったが実際は 14,696 件**で、リリース PR を再取り込みした後の数字を使っていなかった。

**時刻の境界を確かめずに数える。**
`'2026-08-31'::timestamptz` は DB のセッション TZ（UTC）で解釈される。日本時間の朝 9 時間分が
落ちて、8/31〜9/4 のマージを 65 件ではなく 66 件と答えていた。**さらに悪いのは、期待値の側も
UTC で測っていたこと** — 9/2 の問いは「29 件」という誤った期待値に誤った答えが一致して
**合格していた**。数える問いを書くときは、境界をどのタイムゾーンで測ったかを `why` に残す。

**禁止語の列挙は終端が無い。**
「コンソールで作るな」を `mustNot: ["コンソールで直接"]` と書いたら、正しい否定文
「コンソールで直接作成する運用ではありません」を落とした。否定形を足していく方向
（ではありません / しません / 選べません / ではなく…）は収束しない。
**言及ではなく「読み手への推奨の形」を禁じる**と有限になる。この禁止語は誤爆 2 回・的中 0 回だった。

**性能が落ちたと読む前に、失敗の中身を見る。**
judgment 1/10・多ターン 1/15 という結果を見て回答品質の崩壊を疑ったが、
51 問目以降が全部 `You have no credits remaining` だった。**集計だけを見て中身を読まなかった。**

**正解が 1 つに決まらない問いを評価に入れない。**
「監視の方針はいつ、何から何へ変わった？」には (1) 定義方法がコンソール→Terraform と
(2) 監視対象が死活→業務 の 2 つの正しい答えがあり、片方しか期待値に書いていなかった。
**軸を問いの側で固定する**（「アラートの定義方法は…」）。

**背景ジョブを起動しすぎる。**
完了済みの処理を待ち続ける残骸を含め、待機ループを 3 つ同時に走らせていた。

## 運用

```
mitos sync              登録済みの取り込み元をまとめて更新（launchd が毎日 6:00 に実行）
mitos doctor            資格情報と接続、Linear MCP の疎通
mitos advice            編集時の助言が効いているか（ヒット率・再提示率）
mitos usage             OpenAI の使用量
mitos who               誰が誰かの名簿
```

**どこに課金が発生するか（実測でコードを追った）。**

| 経路 | OpenAI | Voyage |
|---|---|---|
| ダッシュボードのチャット | **かかる**（`chat.ts` → `http.ts`） | かかる（検索の埋め込み + rerank） |
| `bun run eval` | **かかる**（`chat.ts` を直接呼ぶ） | かかる |
| MCP の `search_knowledge` | **かからない** | **かかる**（質問の埋め込み 1 回 + rerank 1 回） |
| MCP の `check_path` / 編集フック | かからない | **かからない**（パス完全一致だけ） |
| `mitos sync` などの取り込み | かからない | かかる（各ノードの埋め込み） |

`OPENAI_API_KEY` を使うのは **`src/chat.ts` の 1 ファイルだけ**で、`mcp.ts` も
`hook-check-path.ts` もこれを import していない。**開発中の MCP 呼び出しで OpenAI 課金は起きない。**

**他のプロジェクトからも、そのまま使える。**
プラグインは `~/.claude/settings.json` の `enabledPlugins` で有効にしてあるので
**このマシンの全プロジェクトに効く**（`claude mcp add` は不要。やると二重登録になる）。
範囲の解決は `identify()` が **git remote** で行うので、パスではなくリポジトリで決まる。
実測: `~/Projects/monopoly-source` から `mitos search` を叩いて、そのリポジトリの記録が返った。

日次同期は `~/Library/LaunchAgents/com.mitos.sync.plist`。ログは `~/.claude/mitos-sync.log`。
外すときは `launchctl bootout gui/$(id -u)/com.mitos.sync`。

**Linear は API キーが発行できない**（組織で禁止）ので、OAuth 済みの MCP を `claude -p` の
ヘッドレス実行で叩く。`--output-format stream-json` からツール結果を生で拾うので、
issue 本文もコメントも LLM を通らない。ページ送りのカーソルも呼び出し側が読む。

## いまの回答精度（2026-09-07 実測）

**答えの正しさ 100%（74 問中 74 問）。**`bun run eval` で測る。

| 面 | 実測 | 測り方 |
|---|---:|---|
| 引ける（検索） | recall@5 **93%** | `evals/hybrid.ts`（30 問） |
| **答えが正しい** | **100%**（74/74） | `evals/answers.ts` + `evals/multi.ts` |
| 存在しない根拠を引く | **0 件** | 引用番号が実在するかを機械的に確認 |
| 網羅している | 未測定 | 下の「入っていないもの」参照 |

内訳。**作り方が違うので束を分けている。**

| 束 | 実測 | 中身 |
|---|---:|---|
| `answers.json` | 31/31 | 手書きの事実（GitHub / Linear / DB で裏を取ったもの） |
| `answers-auto.json` | 18/18 | DB から機械生成（複合条件・近い番号の干渉・過去と現在） |
| `answers-judgment.json` | 10/10 | 正解が 1 つに決まらない判断の問い |
| `answers-multi.json` | 15/15 | 多ターン（指示語の解決・訂正の持続・話題の切り替え） |

**100% を額面どおりに読まない。**24 問から 74 問へ広げる過程で落ちた問いは
**7 問中 6 問が評価セットの側の欠陥**だった。いま測れているのは
「自分で書いた期待値に合うか」であって、**問いの選び方の偏りは測れていない**。

### 直した欠陥（74 問化のときに見つかったもの）

チャット側の実欠陥は 3 件だけだった。

- **期間の上限を指定できなかった**（`since` のみ）。`until` を足した
- **日付が UTC で解釈されていた。**`'2026-08-31'::timestamptz` は DB のセッション TZ で
  読まれるので、日本時間の朝 9 時間分が落ちる。日本時間の丸一日として読むよう直した
- **存在しない引用番号を書いた**（20 件しか返っていないのに `[24]`）

残りは全部、期待値の側が誤っていた。**型は 3 つ。**

1. **禁止語が正しい否定文を拾う**（「コンソールで直接作成する運用ではありません」を落とした）
2. **禁止語が近い番号の正当な言及を拾う**（OT-4858 は OT-4856 の子なので挙げて正しい）
3. **正解が 1 つに決まらない問いを入れていた**（「監視の方針」に 2 つの正しい答えがあった）

### 測り方について

**LLM を審判にしていない。**人間との一致は 90% と報告されているが、審判自体の校正が要り、
実行のたびに揺れる。答えには PR 番号・日付・状態という検証可能な語が必ず入るので、
`must` / `mustNot` の突き合わせで足りる。決定的で、無料で、速い。

`re:` で始めた期待値は正規表現になる。**素の部分一致では実体と述語を結び付けられない**ので、
取り違えを見たいときは `re:OT-4856(?:(?!OT-)[^。\n]){0,25}QA` のように、
間に別の実体が挟まらない範囲で束縛する。

多ターンは**対話状態の不変条件**も見る。前に挙げた PR のどれにも触れていなければ話がすり替わって
おり、会話に一度も出ていない PR を混ぜていれば作話している。

**禁止語（`mustNot`）は最後の手段。**否定形を列挙する方向（ではありません / しません /
選べません…）は終端が無く、実測で誤爆 3 回・的中 0 回だった。**非存在の言明を `must` 側で
要求するほうが、境界がはっきりして収束する。**

### ナレッジ DB としての総合評価（2026-09-07）

**答えの正しさだけが 100% で、他の 2 面は測れていないか、低い。**
評価が満点なのは「聞かれたら正しく答えるか」の面だけで、ナレッジ DB の質はそれだけでは決まらない。

| 面 | 点 | 根拠（実測） |
|---|---:|---|
| 聞かれたら正しく答える | **90** | 74/74。ただし**自作の評価セット**なので額面 100 は割り引く |
| 持っている範囲（網羅性） | **55** | ノード 35,074 / 8 作業場所 / 2012〜2026。**コード側はほぼ完全**だが、**Linear issue は 4,393 件中 22 件（0.5%）** |
| 聞かれる前に言う（JARVIS） | **15** | フックは動くが、走った編集 3 回。**質を語れる母数が無い** |
| **総合** | **65** | 上を「今日の役に立ち方」で重み付けした意見 |

**いちばん効くのは網羅性で、その中でも Linear。**依頼者の最重要要件は
「ドメイン知識にめちゃめちゃ強くなってほしい」であり、**業務のドメイン知識はチームの issue に
ある**。いま入っているのは自分が関わった 22 件だけなので、
「なぜその仕様か」「誰がいつ決めたか」の大半をまだ持っていない。

**総合を上げる順番。**
1. 他人の Linear issue 4,393 件（55 → 75 くらいまで動くはず）
2. JARVIS の分母を貯める（15 の面が動かないと総合は 70 台で頭打ち）
3. 評価セットをチャット履歴から作り直す（90 の面の割り引きを外す）

**やってはいけない。**評価が満点だからナレッジ DB として完成した、と読むこと。
測っている面が 1 つだけである。

### まだ弱いところ

- **評価セットが自作。**質問も期待値も、コーパスに何があるか知ったうえで書いている。
  74 問に増やしても**問いの選び方の偏りは測れない**。チャット履歴（`chat` 表）に実際の質問が
  貯まっているので、**そこから作り直すのが次の一手**
- **人名の質問が最弱**（検索の評価で 2/3）。名簿 11 人のうち 3 人（永井さん・矢野さん・
  増田さん）は**ハンドル名の類似だけの推測**でメールの裏を取っていない
- **AI レビューが全体の 28%**（9,358 件）。残すと決めたが、答えの質への寄与を測っていない
- **編集フックの選別が粗い。**距離順に並べるだけ。実測で 3 件中 1 件しか役立たなかった
- **陳腐化は issue と PR にしか効かない。**「そのコードは後で書き換えられた」は検知できない
- **ハルシネーション率は 1 問しか試していない**（存在しない issue）

### 入っていないもの

- **他人の Linear issue。**入っているのは自分が担当か起票の 22 件だけ（チーム全体は 4,393 件）
- **MTG 文字起こしの後半。**貼り付けが 12,000 字で切れる。分割していない
- **monopoly-source 以外のセッション記録**

### やり残し: プロジェクトの削除（紐づくデータごと）

**画面から Project を消せるようにしたい。**退社・案件終了で、そのプロジェクトの知識を
まとめて消す必要が出る。いまの `DELETE /api/groups/:id` は**束を解くだけ**で、
scope も record も node も残る。

消す前に決める必要があること。**FK の向きは実測済み。**

| 対象 | いまの挙動 |
|---|---|
| `group_member` / `scope_group` | 既に消せる（`mitos_cfg` に delete 権限あり） |
| `term` | `group_id` が `on delete cascade` なので束と一緒に消える |
| `record` → `node` | `node.record_id` が cascade。**record を消せば node も消える** |
| `scope` | `record.scope_id` と `node.scope_id` が **`on delete restrict`**。record を先に消さないと scope は消せない |
| `chat` | `scope_ids int[]` で FK が無い。**あとで束が消えても読めるように `scope_name` を持たせてある**ので、消すか残すかは設計判断 |
| `person` | プロジェクトに紐づいていない（全体で 1 つの名簿）。消さない |

**いちばんの論点は鍵。**画面が使う `mitos_cfg` は `record` / `node` に触れない設計で、
「画面が壊れてもナレッジは書き換わらない」がその根拠になっている。削除のために
`mitos_cfg` へ record / node の delete を渡すと、**この境界が消える**。取り得る形は 3 つ。

1. 画面では削除を「予約」だけして、実体の削除は CLI（`postgres` ロール）が行う
2. `mitos_cfg` に **delete だけ**を足す（update / insert は渡さない）
3. 削除専用のロールをもう 1 つ作る

**HNSW は行を消しても縮まない。**大量削除のあとは REINDEX が要る（実測: 334MB → 201MB）。
削除機能には「消したあと何をすべきか」も含めて書く。

### 次にやるなら、この順

1. **プロジェクトの削除**（上の節。退社・案件終了で必要になる）
2. **`created_at` の遡り更新**（monopoly-source 5,432 / ALLADiN 3,348 / asta 3,188 が未設定。
   無いと「作成した最新の PR」が並べられない）
3. **評価セットをチャット履歴の実際の質問から作り直す**（自作バイアスを外す）
4. **編集フックの選別**（`mitos advice` の分母が貯まってから）。**これが JARVIS の本線**
5. 他人の Linear issue 4,393 件（まとめ叩きで 5 時間。**先に compute を 1 時間 XL へ上げるのが得**）
6. 名簿の推測 3 人を裏取り、AI レビューの寄与を測る
