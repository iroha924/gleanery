## 何を変えたか

ダッシュボードを Vercel へ出した。**https://mitos.iroh4.com で動いている**（Clerk の本番インスタンス、
関数は Neon と同じ `sin1`）。あわせて、リポジトリを持たないホストで黙って壊れる機能を
「到達できない」と言う形に直し、資格情報なしで通る CI を足した。

apex の `iroh4.com` は Squarespace のまま。サブドメインを足しただけで既存サイトには触れていない。

## 採った案と棄却した案

- 採用: Vercel の `services` で画面と API を 1 プロジェクトに載せる。棄却: 2 プロジェクト + rewrite — services が使えたので不要になった
- 採用: Neon は direct endpoint のまま。棄却: pooled endpoint — **実測で壊れた**（下の検証）
- 採用: `installCommand` を書かない。棄却: `bun install --production` — 上書きするとコンテナ内で最も古い bun が選ばれ（公式に明記）、`bun.lock` を読めずに落ちた
- 採用: `buildCommand` を置いて Vercel の型検査を飛ばす。棄却: TypeScript の版を下げる — 落ちるのは `@vercel/backends` の側で、tsc は pre-commit と `bun run check` が通している
- 採用: `roots` が空なら `grep_code` / `read_code` を渡さず、指示も差し替える。棄却: 道具は渡して 0 件を返す — 0 件が「探したが無い」と読まれ、読んでいないものを読んだように答える
- 採用: 費用の月額は書けないホストで `null`。棄却: 0 を出す — 読めない合計を $0 と見せることになる
- 採用: `VERCEL_URL` を azp の許可へ足す。棄却: 環境変数で preview の URL を追う — デプロイごとに変わるので入れた瞬間に古くなる
- 採用: `config` の鍵も管理鍵へ落とさない。棄却: `read` だけ弾く（従来） — 手元は `knowledge.env` を丸ごと持つので踏まないが、デプロイ先は 1 変数ずつ入れるので踏む
- 採用: CI はシークレットを置かない。棄却: DB へ繋ぐテストを CI で走らせる — 資格情報を CI に置くことになり、`d-monthly-update-check` の棄却理由と同じ形になる
- 採用: デプロイ手順は skill。棄却: `.claude/rules`（当初の指示） — `rules-audit` が「`paths` 付きだと要る場面で載らない」と指摘し、当たっていた

## 検証

```
bun run check   exit 0（biome + server tsc + dashboard tsc -b）
bun run test    70 pass / 0 fail
```

**pooled endpoint を棄却した根拠。**同時 8 本 x 25 回 = 200 回を 2 巡。

| | `search_path` 消失 | `<#>` の失敗 |
|---|---|---|
| direct | 0 / 0 | 0 / 0 |
| pooled | 19 / 29 | 24 / 35 |

PgBouncer のトランザクションプーリングは文ごとに別のサーバー接続へ振るので、`connect()` が
立てるセッション変数が保たれない。**逐次 1 本では両方 OK に見える** — 最初にそう測って、
危うくそれを結論にするところだった。

**CI が資格情報を要らないこと。**`env -i` で環境を空にして 68 pass / 0 fail / 2 skipped
（DB が要る 2 件は自分で skip する）。ワークフローの全ステップをローカルで同じ順に実行し、
`bun run bundle` が決定的で `plugin/dist` に差分が出ないことも確認した。

**本番の疎通。**画面 200 / deep link `/now` 200 / 鍵なし `/api/now` は 401。
サインイン後に `/now` へデータが出ることは本人が確認した。

**レビューの指摘のうち、実測で確かめて直したもの。**`config` の鍵は `KNOWLEDGE_DB_URL_CFG` を
外して実行し、管理鍵へ落ちずに例外になることを見た。`framework` 未固定で `server/` が
静的配信へ落ちる件は、固定する前に実際に踏んで `evals/` が出力に含まれるのを見ている。

未検証:

- **CI はこの PR で初めて走る。**ローカルで同じ順に通したが、GitHub 上での実行は未確認
- Vercel の Git 連携は繋いでいない。デプロイは CLI から手で叩いている
- `MITOS_ALLOWED_ORIGINS` に本番ドメインだけを入れているので、preview は `VERCEL_URL` の
  経路でしか通らない。ブランチ URL（`VERCEL_BRANCH_URL`）は別ホストなので、Git 連携へ移ったら
  そこが効いてくる

## 見送った指摘

- `~/Projects` の同期走査 217ms（設定画面の 1 操作あたり）: correctness に効かない
- `usageLogWritable` が一度落ちると再起動まで戻らない: 費用表示が出ないだけ
- `.gitignore` の `.env*` が既存の `.env.local` 行を包含: 追跡中のファイルには一致しない
- `http.ts → server.ts` のリネーム自体の要否（`entrypoint` を明示しているので名前は任意）:
  動いているものを戻す理由が無い
- Codex のレーンは 2 巡とも `collab: Wait` で停止して打ち切った。**モデルの多様性は
  この PR では得られていない**

崩れた指摘:

- 「`VERCEL_URL` を足しても狙った preview は救えない」: 実際に preview でサインインして
  `/now` にデータが出ている。静的な読みだけの推定だった

https://claude.ai/code/session_01FXRN532Air36M4zChDwtiD
