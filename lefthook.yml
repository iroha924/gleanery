pre-commit:
  parallel: true
  jobs:
    - name: biome
      glob: "*.{ts,json}"
      run: bunx biome check --write --no-errors-on-unmatched {staged_files}
      stage_fixed: true
    - name: typecheck
      glob: "*.ts"
      run: bunx tsc --noEmit
