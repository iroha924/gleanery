node_modules/
.env
*.log
*.tsbuildinfo
codex-prompt-*.md
server/migrate-*.ts
.review-tmp/
.review-tmp-ir.json
.env.local
.env*
.serena/
# グローバルの ignore に頼ると、他の PC と clone した側で外れる。
.claude/settings.local.json

# scripts/bundle.mjs が publish のときに作る。
plugin/dist/
plugin/db/

# scripts/third-party-notices.mjs が node_modules から作り直す。
plugin/THIRD_PARTY_NOTICES.md

# npm pack が置く。publish する物は bundle から作り直す。
*.tgz
