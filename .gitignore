node_modules/
.env
*.log
*.tsbuildinfo
codex-prompt-*.md
server/migrate-*.ts
.review-tmp/
.review-tmp-ir.json
.env.local
.env*
.serena/
# 許可した command が入る。持ち主のグローバル ignore に頼ると、他の PC と clone した側で外れる。
.claude/settings.local.json

# 配布物は publish のときに作る（scripts/bundle.mjs）。commit には入れない。
plugin/dist/
plugin/db/

# 同梱した依存から作り直す（scripts/third-party-notices.mjs）。commit には入れない。
plugin/THIRD_PARTY_NOTICES.md
