{
  "name": "mitos",
  "private": true,
  "scripts": {
    "dev": "bun run api & bun run dashboard",
    "api": "cd server && node src/http.ts",
    "dashboard": "cd dashboard && vite",
    "check": "biome check . && cd server && tsc --noEmit && cd ../dashboard && tsc -b",
    "test": "cd server && node --test test/*.test.ts"
  }
}
