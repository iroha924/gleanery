{
  "name": "gleanery",
  "private": true,
  "packageManager": "bun@1.4.0",
  "scripts": {
    "setup": "bun install --cwd server --frozen-lockfile && bun install --cwd dashboard --frozen-lockfile",
    "cli": "node server/src/cli.ts",
    "dev": "bun run --parallel --no-orphans api dashboard",
    "api": "bun run --cwd server api",
    "dashboard": "bun run --cwd dashboard dev",
    "build": "bun run --cwd dashboard build",
    "lint": "bunx --bun @biomejs/biome@2.5.12 check .",
    "architecture": "node scripts/check-dashboard-boundaries.mjs",
    "verify:ai": "node scripts/check-ai-config.mjs",
    "typecheck": "bun run --cwd server typecheck && bun run --cwd dashboard typecheck",
    "check": "bun run lint && bun run architecture && bun run verify:ai && bun run naming && bun run pairs && bun run rule-scopes && bun run typecheck",
    "test": "bun run --cwd server test && bun run --cwd dashboard test",
    "verify": "bun run check && bun run bundle && bun run test",
    "bundle": "node scripts/bundle.mjs",
    "db:apply": "node server/src/admin.ts apply",
    "db:migrate": "node server/src/admin.ts migrate",
    "db:roles": "node server/src/admin.ts roles",
    "naming": "node scripts/check-naming.mjs",
    "notices": "node scripts/third-party-notices.mjs --check",
    "pairs": "node scripts/check-pairs.mjs",
    "rule-scopes": "node scripts/check-rule-scopes.mjs"
  }
}
